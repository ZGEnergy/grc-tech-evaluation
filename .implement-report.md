# Implementation Report: PRD 06/04 — Makefile Validate Target & CI Integration

## Summary

Wired the three validation scripts (validate_links.py, validate_chart_manifest.py,
validate_content.py) into the build pipeline via Makefile targets and updated the
GitHub Actions workflow to run validation between build and deploy.

## Changes

| File | Action |
|------|--------|
| `report/Makefile` | Added `validate` and `validate-dev` targets |
| `.github/workflows/deploy-report.yml` | Changed `make build` to `make validate` |
| `report/tests/test_makefile_validate.py` | Created (13 tests) |
| `report/pyproject.toml` | Added `pyyaml>=6.0` dependency |
| `report/uv.lock` | Updated lockfile |

## Makefile Targets

- **`make validate`** — strict mode: runs all 3 validators after build, exits non-zero on first failure
- **`make validate-dev`** — permissive mode: passes `--allow-placeholders` to validate_content.py

## CI Workflow

- Checkout already had `fetch-depth: 0`
- Python 3.12 setup and uv install were already present
- Build step changed from `make build` to `make validate`

## Test Results

13/13 tests pass:

- T-D6.04-01: validate target exists in .PHONY
- T-D6.04-02: validate-dev target exists in .PHONY
- T-D6.04-03: validate depends on build
- T-D6.04-04: validate-dev depends on build
- T-D6.04-05: validate runs all three validators
- T-D6.04-06: validate-dev runs all three validators
- T-D6.04-07: validate-dev uses --allow-placeholders
- T-D6.04-08: strict validate does not use --allow-placeholders
- T-D6.04-09: checkout uses fetch-depth: 0
- T-D6.04-10: Python 3.12 setup present
- T-D6.04-11: make validate step runs before upload
- T-D6.04-12: make -n validate dry run succeeds (integration)
- T-D6.04-13: make -n validate-dev includes --allow-placeholders (integration)

## RESULT

```
status: SUCCESS
prd_id: 04
phase: 06
commit_hash: 2017a230
tests_passed: 13
tests_total: 13
lint_clean: true
mypy_clean: skipped
```
