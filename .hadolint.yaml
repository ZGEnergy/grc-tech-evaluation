ignored:
  - DL3003  # Use WORKDIR to switch to a directory (we use cd for temp switches)
  - DL3008  # Pin versions in apt-get install
  - DL3059  # Multiple consecutive RUN instructions (intentional for layer caching)
