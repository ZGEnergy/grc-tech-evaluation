---
test_id: C-4
tool: gridcal
dimension: scalability
network: SMALL
status: qualified_pass
workaround_class: stable
blocked_by: null
protocol_version: "v10"
skill_version: v1
test_hash: "59241b39"
wall_clock_seconds: 286.37
timing_source: measured
peak_memory_mb: 324.03
convergence_residual: null
convergence_iterations: null
loc: 246
solver: "HiGHS, SCIP"
timestamp: "2026-03-13T04:30:00Z"
---

# C-4: SCUC 24hr on SMALL with HiGHS and SCIP

## Result: QUALIFIED PASS

## Approach

Scaled A-5 (SCUC on TINY) to the SMALL network (ACTIVSg 2000-bus, 544 generators, 3206
branches). Since the SMALL network has no augmented timeseries data, a synthetic 24-hour
load profile was generated by scaling base-case loads by hourly shape factors (0.57--1.00
representing a typical winter weekday profile).

**Method: Sequential snapshot OPF** (each of 24 hours solved independently). The native
time-series OPF driver could not be used for two reasons:

1. **TapPhaseControl enum bug** (v5.6.28): The time-series OPF driver crashes with
   `ValueError: 0 is not a valid TapPhaseControl` on networks with transformers. This is
   the same bug documented in B-4.
2. **Monolithic MILP intractability**: Even if the bug were fixed, the time-series driver
   formulates the 24-hour SCUC as a single MILP with ~13,056 binary variables (544 gens x
   24 hours). HiGHS ran for >25 minutes without producing a result before being terminated.

Each hour was solved via `run_linear_opf_ts(grid, time_indices=None, solver_type=...)` in
snapshot mode, using the documented public API.

## Output

| Metric | HiGHS | SCIP |
|--------|-------|------|
| Converged hours | 24/24 | 24/24 |
| Wall-clock (total) | 142.1 s | 141.8 s |
| Mean solve per hour | 1.73 s | 1.67 s |
| Max solve per hour | 2.20 s | 2.22 s |
| Peak memory | 324 MB | 311 MB |
| Total gen range (MW) | 38,252 -- 67,109 | 38,252 -- 67,109 |
| Committed gens | 430 (constant) | 430 (constant) |
| Cycling generators | 0 | 0 |

**No generator cycling observed.** The SMALL network's base-case generator costs are not
sufficiently differentiated to drive decommitment (similar to the vanilla case39 issue noted
in cross-tool-watchpoints.md). All 430 dispatchable generators remain committed for all 24
hours under both solvers. The generation level varies with load (38,252 MW at the valley
hour vs 67,109 MW at peak), confirming that the OPF is correctly dispatching to match demand.

Both solvers produce identical dispatch and commitment schedules, confirming solver-independent
formulation consistency.

## Workarounds

- **What:** Used sequential snapshot OPF (each hour solved independently) instead of the
  native time-series OPF driver with UC mode.
- **Why:** Two compounding issues: (1) TapPhaseControl enum bug crashes the time-series
  driver on networks with transformers, and (2) the monolithic 24h MILP is computationally
  intractable for 544 generators.
- **Durability:** STABLE -- the sequential approach uses `run_linear_opf_ts` with
  `time_indices=None`, a documented public API function. The snapshot mode is well-tested
  and unlikely to break.
- **Grade impact:** The workaround loses inter-temporal UC coupling (min up/down times,
  ramp rates are not enforced across hours). This means the test demonstrates that 24
  independent hourly OPF solves complete successfully on the SMALL network, but not that
  true multi-period SCUC works at scale. This is a meaningful limitation.

## Timing

- **Wall-clock:** 286.37 s (total for both solvers)
- **HiGHS total:** 142.1 s (24 snapshots)
- **SCIP total:** 141.8 s (24 snapshots)
- **Timing source:** measured
- **Peak memory:** 324 MB (HiGHS), 311 MB (SCIP)
- **CPU cores used:** 1

## Test Script

**Path:** `evaluations/gridcal/tests/scalability/test_c4_scuc_scale_small.py`

Key code showing sequential snapshot approach:

```python
from VeraGridEngine.Simulations.OPF.Formulations.linear_opf_ts import run_linear_opf_ts

for t in range(24):
    grid = load_gridcal(network_file)
    for ld in grid.get_loads():
        ld.P = float(ld.P * LOAD_SHAPE[t])

    opf_vars, model = run_linear_opf_ts(
        grid=grid,
        time_indices=None,  # snapshot mode
        solver_type=solver_enum,
    )
```
